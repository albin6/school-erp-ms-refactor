import { Typography, Card } from 'antd';

const { Title, Paragraph } = Typography;

export const StudentsManagement = () => {
    return (
        <Card>
            <Title level={2}>Students Management</Title>
            <Paragraph>
                This is a placeholder for the Students Management module.
            </Paragraph>
        </Card>
    );
};
